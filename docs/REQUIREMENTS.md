# napflow — Requirements (v0.x)

Status: v0.1 scope adopted 2026-07-02 and implemented; v0.2 scope
adopted 2026-07-11. Every requirement is traceable to a spec:
**FS** = `napflow-flow-schema.md` v0.4 · **EN** = `napflow-engine-spec.md`
v0.2 · **WM** = `napflow-workspace-manifest.md` v0.3 · **YP** =
`yaml-profile.md` · **Dxx** = `DECISIONS.md`. Checkboxes track
implementation; tick them in the PR that lands the behavior (with a test).

Historical v0.1 stages (from AGENTS.md build order — each independently useful):
**S1** loader + models + `napf check` · **S2** engine core + basic nodes +
`napf run` · **S3** remaining nodes + python worker · **S4** server + UI.

## FR-1xx — Workspace & manifest

- [x] FR-101 (S1) Parse & validate `napflow.yaml` into Pydantic models; `napf` locates it by walking upward from cwd. (WM) — models M1, walk-up `core/workspace.py` `find_manifest`/`load_workspace`, `tests/test_workspace.py`, 2026-07-04
- [x] FR-102 (S1) Flow discovery: any directory under `flows.root` containing `flow.yaml` is a flow; identity = workspace-relative path; recursive nesting allowed. (WM §1) — `Workspace.discover_flows` (posix identities, sorted), 2026-07-04
- [x] FR-103 (S1) Env profile discovery: every `envs/*.env` is a profile named by filename stem; no registry. Dialect: `KEY=VALUE`, `#` comments, optional quotes stripped, no `export`/interpolation. (WM §2, EC36) — `Workspace.env_profiles` + `parse_env_file` (dialect pins in WM §2), 2026-07-04
- [x] FR-104 (S2) Env layering: profile file → process environment, last wins. (WM §3) — `layer_env` in `core/workspace.py`; whole process env visible (pinned in WM §3), `tests/test_templating.py`, 2026-07-05
- [x] FR-105 (S2) `defaults.request` merges shallowly into request nodes; templates there may reference only `env.*`/`run.*`. (WM §4, EC23) — request-defaults merge in `core/engine.py` `_effective_request_config` (node keys win wholesale, retry replaces, env/run-only templating), `tests/test_request.py`, 2026-07-05
- [x] FR-106 (S2, v0.1 behavior) Secret masking: values of env vars matching `environments.secrets` patterns (active profile + process env) replaced via substring scan, ≥5-char minimum, at event emission. Declared secrets only. (D22, EN §7) — `SecretMasker` in `core/events.py` (token `***`, longest-first, keys included — pins in EN §7), `tests/test_events.py`, 2026-07-05. Superseded as a v0.2 target by D35/FR-1104: preserve raw local truth and redact presentation/export without rewriting protocol structure.
- [x] FR-107 (S1) `napf init` scaffolds: manifest, `flows/main`, `flows/example` (httpbin demo), `flows/smoke` (fixture→python→assert, offline), `envs/dev.env`, `envs/example.env`, `.gitignore`, `.gitattributes` (`*.yaml`/`*.yml` `text eol=lf`), `.napflow/`. First-touch: `napf run flows/smoke` passes **offline** out of the box (S1 only scaffolds it; the run becomes executable once the S3 node set lands — first-touch green at S3/M3, `test_first_touch_smoke_flow_offline`, 2026-07-06). (WM, EC34) — `cli/scaffold.py` (fixtures/smoke.json added to the WM listing); scaffold checks clean, `tests/test_cli.py`, 2026-07-05
- [x] FR-108 (S3) `python.interpreter` manifest key selects the worker interpreter; `null` = napflow's own. (WM, EN §5a) — `_resolve_interpreter` in `core/engine.py` (resolution rule pinned in EN §5a), interpreter tests in `tests/test_python_node.py`, 2026-07-06
- [x] FR-109 (S1) `codegen:` manifest key is parsed and ignored (reserved). (WM) — `Manifest.codegen: Any`, test in `tests/test_models_manifest.py`, 2026-07-04

## FR-2xx — Flow file format

- [x] FR-201 (S1) `schema: napflow/v1` flow files parse into Pydantic models covering the full v1 node catalog. (FS) — `core/models/`, tests parse the spec example + full-catalog kitchen sink (`tests/test_models_flow.py`), 2026-07-04
- [x] FR-202 (S1) Node ids: `[A-Za-z_][A-Za-z0-9_]*`, unique per flow, human-readable (never UUIDs). (FS, E011) — charset via model pattern (M1), uniqueness via checker E011 (M4), `tests/test_checker.py::test_e011_bad_charset_and_duplicate`, 2026-07-04
- [x] FR-203 (S1) `layout:` is quarantined at the bottom of the file and never affects engine behavior. (FS, YP) — models treat layout as inert metadata; scaffold/canonical emit keep it at file bottom, 2026-07-05; canvas enforcement landed S4/M4 (`merge_flow_document` keeps `layout:` at file bottom; golden layout-only-diff test in `tests/test_flow_merge.py`), 2026-07-07
- [x] FR-204 (S1) YAML read via safe loader only; written through the one shared canonical serializer (block style; strings force-quoted; ints/bools/null bare; no anchors; no line-wrapping; LF+UTF-8; edges as one-line inline maps; fixed schema key order). (YP, D23) — `core/loader.py` `emit_document`/`save_document`, shape tests in `tests/test_roundtrip.py`, 2026-07-04
- [x] FR-205 (S1) Round-trip: load → save preserves comments and key order (ruamel round-trip mode); golden test asserts `emit(parse(emit(x)))` byte-identical and `parse(emit(x))` deep-equals `x`. (YP) — 3-file corpus + checked-in golden, `tests/test_roundtrip.py`, 2026-07-04
- [x] FR-206 (S1) Structures validated against JSON Schema Draft 2020-12 after parse (schema is the type authority). (YP) — via Pydantic, the source of the exported 2020-12 schema (yaml-profile amended at M2), 2026-07-04
- [x] FR-207 (S2) Binary payload envelope: `{"__binary__": true, "content_type", "base64"}`; capture cap applies to encoded form. (FS) — `core/httpclient.py` body codec both directions (decode pins in EN §5), `test_binary_response_envelope`, 2026-07-05
- [x] FR-208 (S1) Write path: the loaded CommentedMap is the single write source — edits mutate the document; Pydantic models are read-only views never serialized back; ruamel line/column marks retained through validation. (YP, EC29) — `LoadedFlow{doc,model}` + `locate()`, positioned-diagnostic tests in `tests/test_loader.py`, 2026-07-04

## FR-3xx — Validation (`napf check`)

- [x] FR-301 (S1) E001–E009 as specified (parse/schema, unknown type/keys, bad edge refs, multi-edge input, missing required input, start/end cardinality, flow-reference cycle with path, broken file refs, Jinja2 syntax). (EN §8) — `core/checker.py` + `diagnostics_from_load_error` mapping, per-code tests in `tests/test_checker.py`, 2026-07-04
- [x] FR-302 (S1) E011 duplicate/invalid node id; E012 reserved port name `error` on End ports and python `outputs`; E010 permanently reserved, never reused. (EN §8, D21) — duplicate/charset via checker+model mapping; E012 in `check_reserved_names`, 2026-07-04
- [x] FR-303 (S1) W101 guard analysis with the strict guarantee: delete guard nodes, test acyclicity; report any remaining cycle with its path. (EN §8, EC16) — `check_guarded_cycles` (guard-removal acyclicity, path reported), 2026-07-04
- [x] FR-304 (S1) W102 port-type mismatch, W103 unconnected error/failed output, W104 unreachable node, W105 env.required key in no discovered profile. (EN §8) — `check_port_types`/`check_unconnected_outputs`/`check_reachability`/`_env_coverage`, 2026-07-04
- [x] FR-305 (S1) W106 unconnected guard exhaustion/timeout port. (D19) — `check_unconnected_outputs` (W106), 2026-07-04
- [x] FR-306 (S1) W107 YAML implicit-coercion lint on unquoted scalars in string-typed fields (hand-edited files). (YP) — `check_templates` W107 branch (scope pinned in EN §8), 2026-07-04
- [x] FR-307 (S1) Python input ports derived by AST-parsing `nodes.py` — `check` never imports user code. (EC14) — `_SurfaceResolver.module_functions` — ast.parse only, literal-default optionality (EC36), 2026-07-04
- [x] FR-308 (S1) `check` runs on the closure of referenced flows; errors block `napf run`, warnings print and proceed. (EN §2, §8) — `check_workspace` closure queue (refs outside flows.root included), 2026-07-04
- [x] FR-309 (S1) Every E/W diagnostic carries file path, line/column (from ruamel marks), node id, and a one-line fix hint. (EN §8, EC29) — `CheckDiagnostic{path,line,column,node_id,hint}` + render(), asserted for every code, 2026-07-04

## FR-4xx — Engine core

- [x] FR-401 (S2) Message-driven scheduler: single asyncio loop per run, `in_flight` accounting, QUIESCENT sentinel enqueued by the zero-reaching decrement. (EN §3, D14) — `FlowRun._pump`/`_dec` in `core/engine.py`, sentinel-race test in `tests/test_engine.py`, 2026-07-05
- [x] FR-402 (S2) Empty-seed guard: finalize immediately when post-seed `in_flight == 0`. (EC08) — `_pump` post-seed return, `test_empty_seed_finalizes_immediately`, 2026-07-05
- [x] FR-403 (S2) Firing rules 1–6 exactly as specified — including merge `all`/`collect` slot-clearing vs rule-2 latest-value retention. (EN §4) — rules 1/5 at S2/M3; rules 2–3 at S3/M1 (`_dispatch`/`_deliver_merge`, TR-1 tests); rule 6 at S3/M3 (fixture auto-seed); rule 4 at S3/M4 (`_deliver_guard`, `tests/test_guards.py`) — all six rules landed, 2026-07-06
- [x] FR-404 (S2) Frames: per-frame variables, inputs, firing counts, guard state; hierarchical frame ids; data crosses only via Start/End. (EN §1) — root frame at S2/M3; hierarchical child frames (per-frame `in_flight` + `done`, path ids, EN §5 pins) at S3/M5, `tests/test_frames.py`, 2026-07-06
- [x] FR-405 (S2) Outcome aggregation: asserts, python-asserts, unhandled error-port messages roll up run-wide; run state = worst outcome in the frame tree. (D20) — root-frame aggregation incl. python-asserts at S3/M2; frame-tree subtree sums + D21 payloads at S3/M5 (`test_nested_subflow_outcomes_bubble_through_subtree`), 2026-07-06
- [x] FR-406 (S2) Run states `passed|failed|error|aborted` per EN §2 definitions, incl. required-End-port failure (D18); exit codes 0/1/2/130. — `_finalize` + `EXIT_CODES` in `core/engine.py` (error_reason vocabulary pinned in EN §2), `tests/test_engine.py`, 2026-07-05
- [x] FR-407 (S2) Message budget (`defaults.run.message_budget`, default 100000): tick per emission run-wide incl. child frames, `budget_warning` at 10%, exhaustion → run `error` naming the hot edge. (EN §3, EC31) — `_tick_budget`, `test_budget_exhaustion_names_hot_edge`, 2026-07-05
- [x] FR-408 (S2) Abort: cancel tasks, close session, state `aborted`; events already written stay valid (dangling `request_started` tolerated). (EN §3, EC20) — abort mechanism + task cancellation landed M3 (`FlowRun.abort`, tested); session close completes at M4 with the niquests session — abort + task cancellation at M3, session close on every exit path at M4 (`FlowRun.execute`), `test_abort_finalizes_as_aborted`, 2026-07-05
- [x] FR-409 (S2) Unhandled error-port message ⇒ run `failed`; nodes without an error port surface evaluation errors as unhandled node errors. (EN §2/§6, EC24) — `_emit_output` error-class check + `_node_error`, `tests/test_engine.py`, 2026-07-05
- [x] FR-410 (S2) Per-firing `max_seconds` settable on any node; the manifest default (`node_timeout_s` 300) auto-applies to `request`/`python` only — `delay`/`loop`/`flow` exempt from the default, explicit value honored. Tripped ceiling → `{error_kind: "timeout"}` on the node's error port; `flow` → child frame `aborted` + implicit error port payload; `loop`/port-less nodes → unhandled node error ⇒ `failed`. (D24) — request/python/port-less paths through S3/M2; flow-aborts-child + subtree cancellation at S3/M5 (`test_flow_timeout_aborts_child_and_keeps_recorded_asserts`), 2026-07-06
- [x] FR-411 (S2) Run deadline: `defaults.run.run_timeout_s` (null = off) + `napf run --timeout N`; expiry cancels in-flight work, finalizes state `error` (exit 2) with `error_reason: run_timeout`, report and JSONL written. (D24) — engine deadline at M3 (`test_run_deadline_expires_with_report`), `--timeout` flag at M5 (`test_timeout_flag_exits_2`), 2026-07-05

## FR-5xx — Node types

- [x] FR-501 (S2) `start` — seeded once per frame; `out` carries the full `inputs` dict; ports define the flow's input interface; port `default:` templates evaluated at BIND with env/run scope only. (FS, EN §4, EC36) — `_seed` + `_bind` in `core/engine.py`, `test_bind_coerces_and_defaults`, 2026-07-05
- [x] FR-502 (S2) `end` — real input ports; accumulates latest value per port; `required: bool` default `true`; required-unwritten ⇒ run failed; `required: false` ⇒ `null`, noted in report; port name `error` rejected (E012, S1 checker). (D18, FS) — pump rule-5 absorb + `_finalize`, TR-3 tests, 2026-07-05
- [x] FR-503 (S2) `request` — niquests shared per-run AsyncSession; `trigger` input; config templating; engine-level retry per node config; non-2xx on `response`, transport failures on `error`; full-detail events. (FS, EN §5, EC13) — `_run_request` + `core/httpclient.py` adapter (NFR-09), non-2xx-is-data/retry/transport-error tests against a local server in `tests/test_request.py`, 2026-07-05
- [x] FR-504 (S2) `condition` — sandboxed Jinja2 expr; forwards incoming message on `true`/`false`. (FS) — `_run_node` in `core/engine.py`, `test_condition_routes_and_forwards_value`, 2026-07-05
- [x] FR-505 (S2) `assert` — check kinds `status`/`expr`/`response_time`, ops `present|equals|not_equals|contains|matches|gt|lt`, `mode: report_all|fail_fast`; emits `assert_result` events; forwards on `passed`/`failed`. (FS) — `_run_assert`/`_eval_check` (present-op + label pins in EN §2), assert test block in `tests/test_engine.py`, 2026-07-05
- [x] FR-506 (S3) `python` — declared inputs only; JSON-serializable I/O; `AssertionError` → error port + report as python-assert; other exceptions → error port with traceback; declared outputs may not be named `error`; params with literal defaults = optional inputs, others required. (FS, E012, EC36) — `_run_python` in `core/engine.py` + worker protocol (return convention pinned in EN §5a; E012 was S1 checker), `tests/test_python_node.py`, 2026-07-06
- [x] FR-507 (S3) `switch` — expr + cases, `default` port, pass-through. (FS) — `_run_node` switch arm (first-match + native-equality pins in EN §5), switch tests in `tests/test_nodes.py`, 2026-07-06
- [x] FR-508 (S3) `merge` — `any` (immediate forward), `all` (rendezvous, clear on emit), `collect` (count-based list). (FS, EN §4) — inline pump rule-3 dispatch (`_deliver_merge`, instant-node + emit-order pins in EN §4), merge test block in `tests/test_engine.py`, 2026-07-06
- [x] FR-509 (S3) `counter` — check-then-decrement: exactly `count` passes on `continue`, then every message → `exhausted`; per-frame reset; optional `reset` input restores count silently. (FS, EC16) — `_deliver_guard` in `core/engine.py` (rule-4 pins in EN §4), counter tests incl. EC16 boundary + silent reset in `tests/test_guards.py`, 2026-07-06
- [x] FR-510 (S3) `timeout` — first-message timestamp; lazy evaluation on arrival; `continue`/`expired`; `reset` clears. (FS) — `_deliver_guard` monotonic clock (boundary pin: elapsed == seconds ⇒ expired, EN §4), `test_timeout_guard_lazy_expiry_and_reset`, 2026-07-06
- [x] FR-511 (S3) `delay` — templatable seconds, cancellable sleep, pass-through. (FS) — pulled forward to S2/M3 (TR-2 needed an async node); `_run_node` delay arm, templated-seconds + cancellation tests in `tests/test_engine.py`, 2026-07-05
- [x] FR-512 (S3) `log` — emits masked `log` event, persisted to JSONL, pass-through. (FS, D13) — `_run_node` log arm + live stderr echo in `napf run` (`_LogEcho`), `test_log_emits_event_and_passes_through` + `test_log_events_echo_to_stderr_masked`, 2026-07-06
- [x] FR-513 (S3) `set`/`get` — frame variable map; `set` forwards written value; `get` fires only on `trigger`. (FS, D17) — `_run_node` set/get arms (unset get = `variable_unset` node error, EN §5 pin), set/get tests in `tests/test_nodes.py`, 2026-07-06
- [x] FR-514 (S3) `fixture` — json/csv from `fixtures/` (csv → list of dicts, header required); read once, cached per run; unconnected `trigger` auto-fires once at frame start. (FS, D17) — `_load_fixture` + `_seed` rule-6 auto-fire (cache/CSV/literal-config pins in EN §5), fixture tests incl. cache-survives-deletion in `tests/test_nodes.py`, 2026-07-06
- [x] FR-515 (S3) `loop` — fires on `trigger`; `over` evaluated against that delivery; child frame per item binding `item`/`index`; `sequential`/`parallel` with `max_concurrency`; iteration error = body frame `failed`/`error`; `on_error` gates scheduling only; `fresh_session` opt-out; `results` (index-ordered)/`errors` outputs. (FS, EN §5, D20, EC36) — `_run_loop` (output/error-entry pins in EN §5), loop tests incl. EC36 parallel ordering + EC06 + cookie isolation in `tests/test_frames.py`, 2026-07-06
- [x] FR-516 (S3) `flow` — child frame; ports derived from target Start/End; implicit `error` port with `{state, failed_asserts, unhandled_errors}`; reference-only semantics (E007 DAG). (D21, FS) — `_run_flow_node` + `_close_child` subtree outcomes (EN §5 pins), subflow tests incl. nesting in `tests/test_frames.py`, 2026-07-06
- [x] FR-517 (S3) `note` — markdown, no ports, no runtime behavior. (FS) — runtime no-op: in `SUPPORTED_NODE_TYPES`, excluded from firing and `nodes_never_fired` (`_finalize`), 2026-07-06

## FR-6xx — Templating

- [x] FR-601 (S2) Jinja2 `SandboxedEnvironment` + `StrictUndefined` is the only expression/template language, for `{{ }}` config strings and bare `expr:` alike. (D10) — `Renderer` in `core/templating.py` (sandboxed string + native envs; `render`/`evaluate`/`render_config`), `tests/test_templating.py`, 2026-07-05
- [x] FR-602 (S2) Context: `env`, `inputs`, `run` (`id`/`timestamp`/`env_name`), `nodes` (frame-local latest, unwrapped), `trigger` (full envelope), `item`/`index` in loop bodies. (EN §6) — `_template_context` in `core/engine.py` (`item`/`index` bind with loops, FR-515/S3), `test_nodes_context_and_trigger_envelope`, 2026-07-05
- [x] FR-603 (S2) Undefined variable → node error to the error port; port-less nodes → unhandled node error, run failed. (EN §6, EC24) — `_node_error` outlet routing (error-port path exercised at M4 with request), `test_undefined_variable_is_unhandled_node_error`, 2026-07-05
- [x] FR-604 (S2) Native-value rule: a config value that is exactly one `{{ expr }}` evaluates to the native value; mixed content renders to string; field schema type applies post-evaluation. (D25) — structural detection + `coerce_value`/`stringify_native` (pins in EN §6); node runners apply field types at M3/M4, `tests/test_templating.py`, 2026-07-05

## FR-7xx — Observability

- [x] FR-701 (S2) JSONL per run at `.napflow/runs/<flow>/<run-id>.jsonl`, append-only, objects identical to the live WebSocket stream; retention per `defaults.run.history`. (D13) — `JsonlSink`/`run_log_path`/`apply_retention` in `core/events.py` (run-id format + JSONL profile pinned in EN §7), `tests/test_events.py`, 2026-07-05
- [x] FR-702 (S2) Event vocabulary exactly per EN §7 (types, common fields, `seq`). — 13 event dataclasses + `EventStream` stamping in `core/events.py`, exact-vocabulary test in `tests/test_events.py`, 2026-07-05
- [x] FR-703 (S2) Full request/response bodies always stored; `defaults.run.body_capture_mb` (10) valve with `truncated: true` marker; `value_preview` truncation in stream-only fields. (D13, EN §7) — full bodies in `request_finished`; per-body valve `_capture_body` (wrapper pins in EN §5); `_preview` truncation, `tests/test_request.py`, 2026-07-05
- [x] FR-704 (S2) Events are born masked (FR-106); `run_finished` carries state, durations, assert tallies, unhandled errors, masked end outputs, `nodes_never_fired`. (EN §7) — masking at M2 (`EventStream.emit`); `run_finished` populated by `_finalize` at M3, `tests/test_engine.py`, 2026-07-05
- [x] FR-705 (S2) Timing breakdown captured where niquests exposes it; fields omitted otherwise. (EN §7) — best-effort `conn_info` mapping in `core/httpclient.py` `_timing_of` (total_ms always, rest omitted when unexposed), 2026-07-05
- [x] FR-706 (S2) Run-level capture valve `defaults.run.run_capture_mb` (500): total body bytes per run capped, excess truncated with marker, `capture_warning` at 10% remaining. (EN §7, EC32) — run-level accumulator in `_capture_body` + `capture_warning` at 10%, `test_run_capture_valve_and_warning`, 2026-07-05

## FR-8xx — CLI

- [x] FR-801 (S1) `napf check` — full rule set over all discovered flows; non-zero exit on E-codes. (WM) — `cli/main.py check` — exit 1 on E-codes, 0 warnings-only, 2 no-workspace, 2026-07-05
- [x] FR-802 (S1) `napf list` — discovered flows with their Start/End ports. (WM) — `cli/main.py list_flows` — identity + Start/End ports, invalid-flow marker, 2026-07-05
- [x] FR-803 (S2) `napf run <flow> [--env NAME] [-i k=v ...] [--input-json] [--timeout N]` — inputs validated & type-coerced against Start ports, fail-fast on unknown/missing; End outputs → stdout as one JSON object; logs → stderr; exit codes 0/1/2/130. (FS, EN §2, D24) — `cli/main.py run` (pins in WM CLI section: run gate, unmasked stdout, input precedence, best-effort default env, Ctrl-C), `tests/test_run_cli.py` + S2 DoD test in `tests/test_request.py`, 2026-07-05
- [x] FR-804 (S2) Report formats `none|junit|json` per `defaults.run.report`. (WM) — `cli/report.py` `write_report` (paths + junit mapping pinned in WM), `test_json_report`/`test_junit_report_counts_match`, 2026-07-05
- [x] FR-805 (S1) `napf init [dir]` per FR-107. (WM) — `cli/main.py init`, refuses existing napflow.yaml, 2026-07-05
- [x] FR-806 (S4) `napf ui [--port]` — serve UI + API + WebSocket on one localhost port, open browser. (WM, D03) — server half (port 6273 + scan, localhost-only, `--no-browser`, stdlib `webbrowser`) at S4/M1; complete at S4/M2 with the real bundle served (`test_ui_bundle_served_with_spa_fallback` + Playwright smoke), 2026-07-06

## FR-9xx — Python worker

- [x] FR-901 (S3) Persistent worker subprocess per flow module, lazy spawn, capped pool; JSON-lines protocol over stdin/stdout; worker imports `nodes.py` once at startup. (EN §5a) — `core/worker.py` + `core/worker_main.py` (cap enforcement deferred to multi-module runs, S3/M5 — EN §5a pin), `test_worker_persists_module_state_across_firings`, 2026-07-06
- [x] FR-902 (S3) Serial task processing (documented limitation: no CPU parallelism through python nodes; stuck firing blocks the module until kill). (EC09) — per-worker `asyncio.Lock` in `core/worker.py`; sequential-task tests in `tests/test_python_node.py`, 2026-07-06
- [x] FR-903 (S3) Timeout: await with node `max_seconds` → `terminate()` → 2s grace → `kill()` → error to node's error port (`error_kind: timeout`) → lazy respawn. (EN §5a) — initial timeout/respawn coverage landed 2026-07-06; v0.2 M2 made abnormal teardown immediate and cancellation-safe, with no late side effect or replacement overlap (`test_timed_out_worker_no_late_side_effect_or_replacement_overlap`), 2026-07-13
- [x] FR-904 (S3) Crash isolation: worker death = node error (`worker_crash`), never an engine failure. (EN §5a) — EOF/fatal handling in `_pump_protocol` + pre-send retry pin (EN §5a), `test_worker_crash_is_node_error_and_respawns` + import-failure test, 2026-07-06
- [x] FR-905 (S3) Protocol integrity: worker `dup()`s the real stdout fd for protocol lines at startup and rebinds `sys.stdout`/`sys.stderr` to capture streams forwarded as log events — user `print()` cannot corrupt the protocol. (EN §5a, EC28) — `core/worker_main.py` (fd 1 additionally pointed at fd 2 for raw-fd writers — EN §5a pin), `test_print_flood_cannot_corrupt_protocol`, 2026-07-06
- [x] FR-906 (S3) Windows: spawn semantics, `CREATE_NO_WINDOW`, reliable terminate. Grandchild processes documented as not reaped (EC22). — `creationflags` in `core/worker.py`; the whole `tests/test_python_node.py` suite (real subprocesses) runs on the Windows CI leg, 2026-07-06

## FR-10xx — Server & UI (S4)

- [x] FR-1001 BlackSheep server is a thin adapter over core: serves static UI bundle, REST for flows/runs, WebSocket for live events; core never imports it. (D03/D04) — `napflow/server` at S4/M1 (REST/WS over `core/runprep.py`; import-linter forbids core→server and server→cli); static bundle real at S4/M2, 2026-07-06
- [x] FR-1002 Canvas (@xyflow/react): render/edit nodes+edges; single-edge-input enforcement on connect; output fan-out; soft port-type coloring + W102 hints. (FS, D11) — RENDER half S4/M3 2026-07-06; EDIT half S4/M4 2026-07-07: drag/connect/delete/add via store edit actions, E004 auto-replace on connect (owner fork; Playwright `editing.spec.ts` asserts ONE edge survives); W102 hints surface via the post-save check diagnostics (badges + panel) AND live at connect time since 2026-07-08 (`ConnectHint.tsx` — panel while dragging over a mismatched typed input, soft/never blocks per D11; Playwright asserts hint + connects-anyway + post-save W102)
- [x] FR-1003 Canvas writes flow.yaml through the shared canonical serializer; layout changes touch only the `layout:` block. (YP) — S4/M4 2026-07-07: `loader.merge_flow_document` (surgical in-place merge; `tests/test_flow_merge.py` — no-op = byte-identical, golden layout-only diff, comment survival) + `PUT /api/flows/*` (validate→etag→merge→`save_document`; `tests/test_server.py` write-path suite); UI never emits YAML
- [x] FR-1004 Filesystem watch: external change → reload or prompt (last-write-wins). (FS) — S4/M4 2026-07-07, v1 shape = etag POLLING (~2s, `GET /api/etags/*`; a native FS watcher is a refinement, not a contract change): clean canvas ⇒ silent live-reload; dirty ⇒ PUT 409 raises the reload/overwrite prompt (`SaveStatus`); Playwright external-change test
- [x] FR-1005 Run on canvas with live event overlay; run history browser replays any JSONL (dangling `request_started` tolerated). (D13, EC20) — S4/M5 2026-07-08 as RUN MODE (D29: editing locks, canvas animates off the M1 WS; root-frame scope, containers pulse for their subtree): `runview.ts` reducer (`runview.test.ts` — 14 tests incl. EC20 `incomplete` + retry/EC13/D19 semantics), run controls w/ hybrid inputs popover (`napf run -i` parity), bottom panel w/ expandable full wire detail + abort + history replay; Playwright `run.spec.ts` (5 e2e: pass/fail/override/abort/EC20-replay, real runs through the real server)
- [x] FR-1006 Start ports editable as key-value list; End required flags editable; check errors/warnings (E/W codes) surfaced on canvas. (FS) — CHECK half S4/M3 2026-07-06 (W-badges + diagnostics panel, Playwright W103/E004); PORT EDITING S4/M4 2026-07-07 (`PortEditor.tsx` — Start name/type/default rows, End required checkboxes; Playwright FR-1006 test asserts the saved model); Start-port defaults type-aware since 2026-07-08 (number/boolean/object/list parse native, red-border local-only on mismatch; Playwright asserts `default: 42` saves as a number)
- [x] FR-1007 Subflow UX: drill-in navigation, "used in N places", clone-to-new-flow action; ghost-wires for cross-node template references. (FS, D09) — S4/M6 2026-07-09: drill-in = double-click or inspector button on flow/loop nodes (pure navigation, browser back returns; statically-known targets only); `used_by` computed server-side (flow-detail payload), listed with per-node places on the flow-header inspector; clone = `POST /api/flows/clone` folder fork + the invoking node REPOINTS to the clone (D31 scope note); ghost-wires from `template_refs` (Jinja2-AST extraction in `templating.referenced_nodes`, dashed view-only edges, never in the model). Tests: `test_templating.py` extraction, `test_checker.py` refs/used_by, `test_server.py` detail+clone, `graph.test.ts` ghost/drillTarget, Playwright `subflow.spec.ts` (5 e2e)

## NFR — Non-functional requirements

- [x] NFR-01 `napflow.core` importable standalone — zero cli/server/UI imports; enforced by an import-linter test. (EN §0) — `tests/test_architecture.py` + contract in `pyproject.toml`, 2026-07-04
- [x] NFR-02 macOS + Windows + Linux from day one (D26): pathlib everywhere, no shell-isms, spawn-safe subprocesses. (CLAUDE.md) — 3-OS CI matrix green since 2026-07-04; Linux promoted to first-class 2026-07-06 (D26, zero platform-conditional code); pathlib enforced via ruff PTH; spawn-safety re-verified as subprocess code lands (S3)
- [x] NFR-03 Distribution: one pip wheel containing the pre-built UI; no Docker, no Node at runtime; installable via `uv tool install napflow`. (D03) — hatchling `artifacts` force-includes the gitignored `src/napflow/server/static`; CI `ui` job + release.yml both GATE on the bundle being inside the built wheel, S4/M2 2026-07-06; ui job CONFIRMED green on all 3 OS at CI run 28785741251, 2026-07-06
- [x] NFR-04 Python 3.12+; Pydantic v2; ruamel.yaml; Jinja2 sandbox; niquests; BlackSheep + uvicorn; Typer. (CLAUDE.md stack) — final pieces (BlackSheep + uvicorn + websockets) in production use at S4/M1 (`napflow/server`, `napf ui`), 2026-07-06
- [x] NFR-05 Security posture: sandboxed Jinja2 only (no eval), safe YAML loading only, secrets masked at emission, worker subprocess isolation with kill ceiling. (D10/D12/D22/D23) — every clause landed with its component: SandboxedEnvironment is the only template path (S2/M1, `test_templating.py`), safe ruamel profile is the only loader (S1/M2, D23, `test_loader.py`/`test_roundtrip.py`), `SecretMasker` at emission (S2/M2, `test_events.py` + `test_run_cli.py`), worker timeout-kill-respawn (S3/M2, `test_python_node.py`); tick recorded at S4 close 2026-07-09 — the aggregate box was an oversight, not missing work
- [x] NFR-06 Determinism: identical logical flow ⇒ byte-identical emitted file, cross-platform (golden round-trip test in CI). (YP) — checked-in golden + `.gitattributes`, green on 3-OS CI since M2, 2026-07-05
- [x] NFR-07 Apache-2.0 + NOTICE; no CLA; DCO when external contributors appear. (D16) — LICENSE + NOTICE landed 2026-07-04; DCO deferred until external contributors
- [ ] NFR-08 Engine overhead assumptions hold: pipe round-trip and scheduling negligible vs HTTP; parallel loops bounded by `max_concurrency`. (EN §5a) — the bound half is implemented (`max_concurrency` semaphore, `engine.py`, S3/M5), but the review found unbounded task allocation and scheduler starvation that make the old measurement target incomplete. Resolution moved to v0.2 M0/M7: baseline before the lifecycle refactor, then an opt-in perf suite measures the corrected design (NFR-18).
- [x] NFR-09 HTTP client isolated behind one internal adapter module in `core/` — no direct niquests imports elsewhere; swapping the client stays a contained change. (review 2026-07-02) — `core/httpclient.py` sole importer, guarded by `test_niquests_isolated_to_the_adapter`, 2026-07-05
- [x] NFR-10 Dependency-compat CI job: install napflow into a venv alongside `requests` + `botocore` and run the test suite — guards the niquests/urllib3-future conflict class in users' pytest envs. (review 2026-07-02) — `compat` job in ci.yml landed 2026-07-05, but its suite-rerun half was red from its first CI execution (run #10; the module-level import-linter import broke collection in the dev-less venv) — genuinely green since `pytest.importorskip` fix, CI run #19, 2026-07-06
- [x] NFR-11 Changelog: conventional commits + git-cliff (`cliff.toml` committed); `CHANGELOG.md` in Keep a Changelog format, regenerated per release starting v0.1.0. (PLAN M0) — landed 2026-07-04, regenerate with `uvx git-cliff -o CHANGELOG.md`

## Test requirements (priority order — highest bug-risk first)

- [x] TR-1 Merge semantics under fast cycles: `all` clears slots vs rule-2 latest-value retention. (EN §4) — `all` clear/stall/refill + rule-2 retention-with-refire + collect batching + fast-cycle budget backstop through inline merge, merge test block in `tests/test_engine.py`, 2026-07-06; guarded-cycle variant re-exercised at S3/M4 (`test_guarded_cycle_terminates_by_exhaustion` + flagship polling tests)
- [x] TR-2 Quiescence detection: sentinel race + empty-seed finalize. (D14, EC08) — `test_quiescence_under_overlapping_async_firings` + `test_empty_seed_finalizes_immediately`, 2026-07-05
- [x] TR-3 Required-End-port failure path: unreached required output ⇒ `failed` ⇒ exit 1, across subflow and loop frames. (D18, D20) — root-frame half at S2/M3; child-frame half at S3/M5 (`test_child_required_end_unwritten_fails_run`; loop iterations fail the same way via `_close_child`), 2026-07-06
- [x] TR-4 Guard exhaustion routing: `exhausted`/`expired` as pass-through outputs; W106; counter N-passes boundary (Nth vs N+1th message). (D19, EC16) — boundary/zero-count/unwired-exhausted-drops tests in `tests/test_guards.py` (W106 was S1 checker); flagship gives-up variant proves tripping-is-data end to end, 2026-07-06
- [x] TR-5 Guard reset / per-frame isolation in loops & subflows. (EN §4) — silent reset at S3/M4 (`test_counter_reset_restores_count_silently`); per-frame isolation at S3/M5 (`test_subflow_invocations_get_fresh_frames`, `test_loop_guard_state_is_per_iteration`), 2026-07-06
- [x] TR-6 Worker lifecycle: timeout-kill-respawn, crash isolation, Windows semantics. (EN §5a) — timeout-kill-respawn + crash-respawn + import-failure + missing-interpreter tests in `tests/test_python_node.py` (Windows via the 3-OS CI matrix), 2026-07-06
- [x] TR-7 Loader round-trip: comments & key order preserved; golden byte-identity corpus. (YP) — landed at S1/M2 2026-07-04 (`tests/test_roundtrip.py`: 3-file corpus + checked-in canonical golden, LF-pinned via `.gitattributes`, byte-identity green on the 3-OS matrix since); the box was an oversight, ticked at S4 close 2026-07-09
- [x] TR-8 Timeout routing: request/python timeout → error port (wired = run passes, unwired = failed); flow timeout → aborted child + implicit error payload, recorded child asserts still aggregate; loop timeout → run failed via EC24; run deadline → `error`/exit 2 with report written; container nodes NOT killed by the default ceiling. (D24) — request paths S2/M4; python + payload `max_seconds` S3/M2; flow-aborts-child S3/M5 (`test_flow_timeout_aborts_child_and_keeps_recorded_asserts`); loop ceiling = EC24 unhandled path (same `_fire` route as the portless-node test); default-ceiling exemption `test_default_ceiling_exempts_delay`, 2026-07-06
- [x] TR-9 Windows integration: a python-node flow runs through the `napf ui` server (Proactor event loop + subprocess pipes must coexist with the ASGI/WebSocket stack); worker `print()` flood does not corrupt the protocol. (EC28, EC33) — print-flood half green at S3/M2 (`test_print_flood_cannot_corrupt_protocol`); through-the-server half `test_tr9_python_flow_through_real_uvicorn_server` (real uvicorn socket + niquests + websockets client, runs on the whole 3-OS matrix), S4/M1 2026-07-06; Windows leg CONFIRMED green at CI run 28785741251 (job `test (windows-latest)`), 2026-07-06
- [x] TR-10 Native-value rule (D25): exact single-expression detection (surrounding whitespace tolerated; any mixed content ⇒ string render); dict/list/number/bool/null preserved through a request-body round-trip; post-evaluation schema-type coercion (string-typed field stringifies, object-typed field rejects a scalar); bare `expr:` unaffected. — detection/coercion at M1, request-body round-trip + post-eval stringify at M4 (`test_native_body_round_trip`, `test_templated_url_headers_query`), 2026-07-05

## v0.2.0 functional requirements — full-fidelity hardening and replay

These boxes start unticked. Tick only in the PR that lands the behavior
and its named regression/integration tests. v0.1 behavior remains
documented above; D33 permits deliberate breaking changes during v0.x.

- [ ] FR-1101 Versioned run-history envelope/manifest: identifies event/blob/index format, canonical `seq` ordering, inline threshold policy, and hash/byte rules. Unknown newer formats fail clearly or open metadata-only; derived indexes are rebuildable. (D33/D34, PLAN M0)
- [ ] FR-1102 Full-fidelity content store: small values inline; large text/binary/structured values stored once in immutable content-addressed blobs referenced from every persisted payload location. Runtime values are unchanged; no silent truncation. (D34, EC32, PLAN M4)
- [ ] FR-1103 Prepared-request observability: persisted request detail reflects the final effective URL/query, headers/cookies policy, body/blob reference, retries, timing, and response—not a 512-character pre-transport preview. (D34, EC50, PLAN M4)
- [ ] FR-1104 Redaction views: canonical local history preserves real values with private permissions; structural event fields are immutable; terminal/report/export policies redact declared secrets without corrupting schema; CI export defaults safe and raw export is explicit. (D35, EC45, PLAN M4)
- [ ] FR-1105 Self-contained run export/import: bundle manifest + JSONL + referenced blobs; hashes verified; imported runs read-only; redacted export never carries unreachable raw content; explicit CI hard-limit omissions include reason/hash/size. (D34/D35, PLAN M4)
- [ ] FR-1106 Scalable replay: versioned streaming/paged API, sequence/frame/node/event filters, rebuildable seek index, lazy blob fetch, bounded/virtualized UI records, reconstructable frame drilldown, and timeline scrubber/playback without re-execution. (D34/D36, PLAN M5)
- [x] FR-1107 Central workspace resolver covers entry flows, subflow/loop refs, fixtures, histories, source reads/writes, and clone destinations with lexical validation, symlink-aware resolved containment, and run-id validation. (D37, EC38, PLAN M1) — `WorkspaceResolver` plus checker/engine/CLI/server regressions, including parent/final/source/history symlinks and stable `workspace_boundary`, 2026-07-12
- [x] FR-1108 Local server boundary: loopback-only bind, loopback Host validation, and same-origin mutation/WebSocket checks; no accounts/session/OAuth/public bind in v0.2. Workspaces remain explicitly trusted code. (D37, EC51, PLAN M1) — HTTP mutation matrix + pre-accept WebSocket Host/Origin tests, 2026-07-12
- [x] FR-1109 Durable source writes: same-directory temporary write + flush + atomic replace + preserved permissions, with per-file serialized ETag-check/write, shared by flow YAML and nodes.py. (EC46, PLAN M1) — atomic fsync/replace failure tests, yielding lock/concurrent ETag tests, and durable clone/scaffold coverage, 2026-07-12
- [x] FR-1110 Reliable editor persistence: serialized canvas/code save queue; edits during an in-flight save are not lost; navigation, code-editor close, and unload flush or prompt; stale responses cannot overwrite current state. (EC46, PLAN M1) — coordinator race tests + Playwright navigation/close/unload/conflict cases, 2026-07-12
- [x] FR-1111 Portable flow identity transport: identity grammar or correct per-segment URL encoding/decoding supports nested names and reserved characters consistently across API, deep links, history, clone, and browser navigation. (EC49, PLAN M1) — segment-codec unit tests + `/flow/` namespaced deep-link/API/back-forward browser coverage, 2026-07-12
- [ ] FR-1112 Public pytest API: `from napflow.core import run_flow` and the equivalent workspace-bound object surface (`workspace.flow(identity)`, fresh discovery, runtime `workspace.flows` catalog, reusable `Flow.run`/async run) share one preparation/execution/cleanup implementation, create isolated runs, require no server/CLI import, handle exact/nested/non-identifier flow names without ambiguous normalization, and work from the installed wheel. Runtime attribute discovery does not promise generated static types. (D38, EC42, PLAN M6)
- [ ] FR-1113 Deterministic distribution: every documented install path produces a wheel containing the UI; clean Git archive/sdist build and `napf ui` smoke are tested, or unsupported source/Git installation is removed from user documentation. (EC44, PLAN M6)
- [ ] FR-1114 Visual-schema parity: every authoritative editable safety/template field—including universal `max_seconds` and templated typed request/Start fields—has a valid UI representation or explicit YAML-only designation guarded by a schema-to-form test. (EC49, PLAN M6)

## v0.2.0 non-functional requirements

- [x] NFR-12 Scheduler fairness: bounded ready-queue batches yield cooperatively and check monotonic abort/deadline state; a valid inline cycle cannot starve the event loop or report `passed` after its deadline. This closes EC27's cooperative-scheduler half, not synchronous-template preemption. (D36, EC27/EC43) — 128-delivery pump batches + monotonic/quiescence deadline checks; guarded-cycle deadline/abort/heartbeat regressions, 2026-07-13
- [x] NFR-13 Lifecycle cleanup and worker safety: all owned resources close under normal completion, abort, timeout, server shutdown, and external coroutine cancellation; worker timeout is terminate→grace→kill; D32 JSON-lines handles the documented maximum without leaking reader exceptions. Descendant processes remain EC22 after v0.2. (D36, EC09/EC22/EC43) — one shielded run cleanup task + TR-15 resource matrix; immediate worker reap/no-overlap and 16 MiB protocol ceiling with 70 KiB/10 MiB, stderr, malformed, and oversize regressions, 2026-07-13
- [ ] NFR-14 Bounded active state: loop tasks/frames, server subscriber queues, CLI report buffers, replay responses, and browser records scale with configured concurrency/window sizes rather than total input/history length. Durable content remains available on disk. (D36, EC47) — **PARTIAL 2026-07-13 (M3 active-state surfaces met):** fixed loop workers/frame release include 200-item ordering plus opt-in 100k concurrency evidence and abort replay coverage. CLI reports use bounded durable-log passes (including a 100k-assertion disk-backed JUnit memory regression). The server retains no live prefix, caps readers/queues, performs exact disk resync with per-send no-progress deadlines, uses cross-process filesystem reader leases, streams WebSocket replay, and stores scalar finished summaries. Browser pending records and its event window are bounded with a 100k fold regression. The full REST replay response remains open for M5 paging/lazy history, so the cross-milestone NFR stays unchecked.
- [ ] NFR-15 Content reliability: local defaults prefer complete inspectable data; hard limits are explicit and produce unmistakable omission metadata; blob/hash corruption and absence are surfaced, never replaced with plausible partial data. (D34, PLAN M4)
- [ ] NFR-16 Authoritative release gate: reusable PR/tag validation includes Python lint/imports/tests, Vitest, production UI build, installed-wheel smoke, and Playwright; tag equals package version and `.dev` cannot publish; frontend third-party notices audited. (PLAN M7)
- [ ] NFR-17 Experimental compatibility is explicit: all v0.x releases may break flow/event/API formats; `napflow/v1` is not stable until package v1.0; implemented v0.2 formats are versioned and release notes describe breaks/best-effort readers. (D33)
- [ ] NFR-18 Measured performance: opt-in, CI-excluded suite records scheduler/worker overhead, bounded loop peak, full-fidelity storage throughput, and replay memory/first-render against M0 baselines; correctness and content reliability remain primary. (D36, PLAN M0/M7)

## v0.2.0 test requirements

- [ ] TR-11 Public/distribution contracts: documented `run_flow` import executes from an installed clean wheel; clean source/Git build either contains/serves the UI or is rejected/documented unsupported; tag/version mismatch test fails release.
- [x] TR-12 Workspace and server boundary matrix: entry/ref/loop/fixture/history/read/write/clone reject absolute, `..`, drive, malformed run-id, and symlink escapes on macOS/Windows/Linux; normal nested identities and encoded reserved characters work; mutation and WebSocket routes reject foreign Host/Origin while loopback requests pass. — portable resolver/server/engine regression matrix (symlink cases skip only where the platform cannot create them) + browser reserved-route cases, 2026-07-12
- [x] TR-13 Scheduler fairness: tight guarded inline cycle respects millisecond-scale deadline within a documented tolerance, abort is observed, another event-loop task makes progress, and message-budget/quiescence behavior is unchanged. Synchronous Jinja preemption remains explicitly outside this closure (EC27/EC35). — `test_inline_cycle_respects_deadline` + `test_inline_cycle_observes_abort`; existing budget/quiescence suite remains green, 2026-07-13
- [x] TR-14 Worker protocol/timeout: 70KB+ result and long stderr cannot crash/hang close; timeout immediately prevents a delayed side effect, replacement never overlaps, and oversize/malformed protocol data becomes a stable node error. This closes worker-process behavior, not descendant cleanup (EC09/EC22/EC43). — converted M0 worker probes + protocol/normal-close/long-stderr/malformed/oversize pool tests; 10 MiB perf probe succeeds, 2026-07-13
- [x] TR-15 Cancellation cleanup: cancel during delay, HTTP, Python, flow, loop, and finalization leaves no firing task, session, stream, worker, or incomplete owned file beyond the explicitly valid JSONL prefix. — `test_cancellation_cleanup.py` matrix + strengthened real-worker M0 probe; includes shielded cleanup, final emission, fresh sessions, and server shutdown, 2026-07-13
- [ ] TR-16 Full-fidelity round-trip: large text/binary/structured response routed through request event, Log, End, report, export, and replay has one stored blob/hash and byte-identical retrieval; runtime value remains complete; hard-limit omission is explicit.
- [ ] TR-17 Redaction integrity: declared secret values including `passed` and `error` never alter event/state/key vocabulary; raw local inspection remains exact; redacted report/export contains no declared secret or raw orphan blob.
- [ ] TR-18 Replay/history scale: 100k-event run streams/pages with bounded server/browser memory, lazy blobs, deterministic timeline seek, >64KB final summary recognized, missing/corrupt blob explicit, and EC20 incomplete prefix preserved. — **PARTIAL 2026-07-13:** arbitrary-size backward final-record reading, partial-tail tolerance, locked monotonic ordering across equal/backward clocks, canonical-tail validation, and post-completion whole-unit retention are green; paged server/browser replay, lazy blobs, seek, and missing/corrupt blob behavior remain M4–M5.
- [x] TR-19 Loop/frame bounds: 100k-item loop creates only bounded active tasks/frames, honors `max_concurrency`, preserves result ordering/outcomes, and completed child frames remain reconstructable/drillable from events. — 200-item/concurrency-4 regression proves ordered outcomes, release, and one indexed `frame_finished` per item; opt-in `test_parallel_loop_active_tasks_and_frames_100k` proves exactly 16 fixed helpers/at most 16 live Frames across all 100k summaries; abort regression proves only active-concurrency children start and the durable tail is `aborted` without false child completion, 2026-07-13
- [x] TR-20 Persistence durability: immediate flow navigation, code-editor close, unload, edit-during-save, two concurrent saves, simulated interrupted/disk-full write, and external ETag change neither lose accepted edits nor corrupt source; comments/golden diff preserved. — persistence Vitest + Playwright lifecycle/conflict suite, concurrent server PUTs, atomic failure/clone cleanup, and full round-trip suite, 2026-07-12
- [ ] TR-21 Contract validation: checker/worker agree on async and positional-only function support/rejection; malformed binary envelopes route through request error semantics; prepared request capture reflects query/default headers/body policy. — **PARTIAL 2026-07-13:** M2 rejects async/positional-only callables consistently and routes strict binary-envelope failures as non-retryable `request_encoding`; prepared-wire request capture remains M4/FR-1103/EC50.
- [ ] TR-22 Release/compat matrix: Linux Python 3.12/3.13/current plus 3-OS user path, dependency compatibility, complete frontend suites including the FR-1114 schema-to-form parity check, installed-artifact smoke, format-version errors/read-only import, and generated third-party notice check.
